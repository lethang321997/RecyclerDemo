<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="popup_item_menu">
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@color/white</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingTop">@dimen/padding_vertical_option</item>
        <item name="android:paddingEnd">@dimen/margin_start_view</item>
        <item name="android:paddingStart">@dimen/margin_start_view</item>
        <item name="android:paddingBottom">@dimen/padding_vertical_option</item>
    </style>

</resources>
